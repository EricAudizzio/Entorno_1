language: python
python:
  - "3.9"


addons:
  sonarcloud: true


script:
  - pytest
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -Dsonar.organization=EricAudizzio -Dsonar.projectKey=ericaudizzio -Dsonar.sources='./Calculator' -X

notifications:
  email:
    recipients:
      - erixaudixio@gmail.com