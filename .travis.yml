language: python
python:
  - "3.9"


addons:
  sonarcloud: true


script:
  - pytest
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -Dsonar.organization=EricAudizzio -Dsonar.projectKey= Calculator -Dsonar.sources='C:\Users\user\Desktop\proyecto_is\Entorno_1' -X

notifications:
  email:
    recipients:
      - erixaudixio@gmail.com